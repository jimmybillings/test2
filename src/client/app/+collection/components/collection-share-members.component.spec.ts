import { CollectionShareMembersComponent } from './collection-share-members.component';

export function main() {
  describe('Collection Share Members Component', () => {
    let componentUnderTest: CollectionShareMembersComponent;

    beforeEach(() => {
      componentUnderTest = new CollectionShareMembersComponent();
    });

    it('***** HAS NO FUNCTIONALITY TO TEST *****', () => {
      expect(true).toBe(true);
    });
  });
}
